[
    {
        "settingId": "keyboard.dispatch",
        "desc": " Controls the dispatching logic for key presses to use either `code` (recommended) or `keyCode`.",
        "setting": {
            "keyboard.dispatch": "code"
        }
    },
    {
        "settingId": "keyboard.mapAltGrToCtrlAlt",
        "desc": " Controls if the AltGraph+ modifier should be treated as Ctrl+Alt+.",
        "setting": {
            "keyboard.mapAltGrToCtrlAlt": false
        }
    }
]